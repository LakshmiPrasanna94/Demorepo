package org.demo;
import org.demo.LoanType;
rule "set as personal" ruleflow-group "LoanTypeRule"
no-loop
when
	 $p:LoanType(personal==true)
then
	modify($p){
		setPersonal(true);
			}
 end

rule "set as strategic" ruleflow-group "LoanTypeRule"
no-loop
when
	 $p:LoanType(strategic==true)
then
	modify($p){
		setStrategic(true);
			}
 end

rule "set as corporate" ruleflow-group "LoanTypeRule"
no-loop
when
	 $p:LoanType(corporate==true)
then
	modify($p){
		setCorporate(true);
			}
 end
rule "set as other" ruleflow-group "LoanTypeRule"
no-loop
when
	 $p:LoanType(other==true)
then
	modify($p){
		setOther(true);
			}
 end